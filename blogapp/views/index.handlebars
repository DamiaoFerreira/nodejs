<div class="jumbotron">
  <h1 class="display-6">Bem-vindo ao Blog do Node</h1>
  <p class="lead">Este é um BLOG, desenvolvido com Node.js + Express + MongoDB.</p>
  <hr class="my-4">
  <p>Aqui voccê vai poder ler sobre as novidades do mundo da computação.</p>
  <p class="lead">
    <a class="btn btn-primary btn-lg" href="#" role="button">Crie uma conta</a>
  </p>
</div>
<hr/>
<h2>Postagens recentes</h2>
{{#each postagens}}
<div class="card">
    <div class="card-body mt-4">
        <h3>{{titulo}}</h3>
        <p>{{descricao}}</p>
        <a href="/posts/{{_id}}"><button class="btn btn-success">Leia mais</button></a>
        <hr/>
        <small>Categoria: {{categoria.nome}}</small><br/>
        <small>Data de publicação: {{data}}</small>
    </div>
</div>
{{else}}
<h5>Nenhuma postagem</h5>
{{/each}}