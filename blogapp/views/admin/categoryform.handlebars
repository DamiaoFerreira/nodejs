{{#each erros}}
<div class="alert alert-danger">{{texto}}</div>
{{else}}
{{/each}}
<h3>Nova categoria</h3>
<div class="card">
  <div class="card-body">
    <form action="/admin/category/add" method="post">
      <label for="nome">Nome:</label>
      <input type="text" name="nome" placeholder="Nome da categoria" class="form-control">
      <label for="slug">Slug:</label>
      <input type="text" name="slug" placeholder="slug" class="form-control">
      <br>
      <button type="submit" name="addcategoria" class="btn btn-success btn-block">SALVAR</button>
    </form>
  </div>
</div>
